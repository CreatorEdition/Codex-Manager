# 混合模式统计展示与 GPT-5.6 定价

## 状态

✅ 已完成

## 变更

- 首页不再根据 Codex Profile 的“账号直连模式”遮挡请求统计、Token、费用、活跃账号和管理员用量分析。
- 请求日志页不再展示切换网关提示、“仅网关流量”标签或直连模式专用空状态。
- 新增 `gpt-5.6-sol`、`gpt-5.6-terra`、`gpt-5.6-luna` 标准与长上下文官方价格种子。
- 价格种子版本更新为 `2026-07-11`，确保已有数据库自动写入新增规则。
- 标准上下文 Cache writes 单价写入现有 `cache_write_5m_price_per_1m` 字段；当前请求 usage 不提供 cache-write token 分桶，因此费用日志暂不单独计入该项。

## 验证

- `cargo test -p codexmanager-service resolves_gpt_5_6_standard_and_long_context_prices`：通过。
- `cargo fmt --all --check`：通过。
- `git diff --check`：通过。
- 前端 TypeScript 门禁尝试运行，但 pnpm 的依赖策略检查因现有 ignored build scripts 返回失败；未批准或执行这些依赖脚本。
