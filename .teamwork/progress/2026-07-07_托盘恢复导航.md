# 2026-07-07 托盘恢复导航

## 状态

已完成。

## 实施内容

- 语义移植上游 `99bb0c2d`，修复桌面端轻量关闭后从托盘/单实例唤起恢复主窗口时的导航状态。
- 未直接复制上游窗口状态机；仅在 CE 当前 `show_main_window` / `ensure_main_window` 路径中记录主窗口是否曾存在。
- debug 模式下重建主窗口后显式导航到 `http://127.0.0.1:3005/`；release 模式保持 no-op，避免影响打包页面。

## 验证结果

- `cargo test --manifest-path apps/src-tauri/Cargo.toml recreated_main_window_navigation_only_runs_for_debug_recreate -- --nocapture`：通过。
- `cargo check --manifest-path apps/src-tauri/Cargo.toml`：通过。