# 2026-07-07 推广残留清理与上游差异复核

## 状态

已完成。

## 已完成

- 已读取 `.teamwork/sync/status.json`，当前协作状态为 `completed`。
- 已拉取 `origin` 与 `upstream`，确认 `upstream/main` 最新为 `f3efb3a2`。
- 已复核上游近期功能差异，确认部分功能已语义移植，剩余项需要逐项剥离推广内容后评估。
- 已删除 `/author/` 作者/赞助页、专用测试、sponsor 工具、赞助静态资源，并移除 runtime `authorContentUrl` 与 Tauri CSP 白名单。

## 验证结果

- `node --test tests\runtime-capabilities.test.mjs`：6 项通过。
- `apps` 下 `tsc --noEmit`：通过；首次失败由本地 `.next/types` 旧缓存引用已删除 `/author` 路由导致，清理 `.next` 后通过。
- `git diff --check`：通过。
- 作者/赞助残留复扫：仅剩 `crates/web` 的“不暴露 authorContentUrl”测试断言。