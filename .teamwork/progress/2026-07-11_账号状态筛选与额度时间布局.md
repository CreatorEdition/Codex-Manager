# 账号状态筛选与额度时间布局

## 状态

✅ 已完成

## 变更

- 状态下拉框新增“不可用”，直接使用后端已有的 `unavailable` 精确筛选。
- Refresh Token 撤销、过期、重复使用及用量接口 401/403 等已落为 `unavailable` 的账号可单独查看。
- 可用账号 SQL 排除空状态和 `unknown`，避免未确认健康的账号计入可用。
- 额度重置日期与相对倒计时改为分行显示，窄卡片中不再横向挤压叠加。
- 账号计划类型仍由后端 `planTypes` 全量 facet 返回；包括 K12 在内的非预置类型继续按原值显示。

## 验证

- `cargo test -p codexmanager-core active_available_accounts_exclude_unknown_status`：通过。
- `cargo test -p codexmanager-service rpc_account_list_active_filter_uses_backend_filtered_pagination`：通过。
- `cargo test -p codexmanager-service rpc_account_list_supports_backend_plan_status_and_global_sort_filters`：通过。
- `apps/node_modules/.bin/tsc.cmd -p apps/tsconfig.json --noEmit`：通过。
- `cargo fmt --all --check`、`git diff --check`：通过。
