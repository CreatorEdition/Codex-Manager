# 2026-07-07 平台密钥今日用量统计

## 状态

已完成。

## 实施内容

- 语义移植上游 `4ba9a139`，为 `apikey/usageStats` 增加 `todayTokens` 与 `todayEstimatedCostUsd`。
- 保留 CE 当前按 `keyIds` 限载的统计路径；当前页只统计当前页平台 Key，成员只统计其有权访问的 Key。
- 平台密钥表格“Token / 金额”列新增“今日 / 累计”两段展示。

## 验证结果

- `cargo test -p codexmanager-core --lib api_key_usage_stat_summary_serialization_uses_camel_case -- --nocapture`：通过。
- `cargo test -p codexmanager-service --test rpc rpc_apikey_usage_stats_filters_requested_key_ids -- --exact --nocapture`：通过。
- `apps` 下 `tsc --noEmit`：通过。
- `cargo fmt --all --check`：通过。
- `git diff --check`：通过。